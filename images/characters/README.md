# 画像置き場

キャラごとのフォルダに画像を置くと、図鑑（index.html）に自動で表示されます。

- `main.png` — 一覧カードのサムネイル
- `01.png` 〜 `12.png` — 詳細モーダルのギャラリー

例:

```
images/characters/001_jin/main.png
images/characters/001_jin/01.png
```

フォルダ名は `<番号>_<名前の小文字スラッグ>` です（既に全38キャラ分作成済み）。
